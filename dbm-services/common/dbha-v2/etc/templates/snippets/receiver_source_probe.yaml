    - name: {{RECEIVER_SOURCE_PROBE_NAME}}
      enable: {{RECEIVER_SOURCE_PROBE_ENABLE}}
      endpoint: "{{RECEIVER_SOURCE_PROBE_ENDPOINT}}"
      netDialTimeout: {{RECEIVER_SOURCE_PROBE_NET_DIAL_TIMEOUT}}
      netReadTimeout: {{RECEIVER_SOURCE_PROBE_NET_READ_TIMEOUT}}
      netWriteTimeout: {{RECEIVER_SOURCE_PROBE_NET_WRITE_TIMEOUT}}
      user: "{{RECEIVER_SOURCE_PROBE_USER}}"
      password: "{{RECEIVER_SOURCE_PROBE_PASSWORD}}"
      mechanism: "{{RECEIVER_SOURCE_PROBE_MECHANISM}}"
      topics: {{RECEIVER_SOURCE_PROBE_TOPICS}}
      bufferSize: {{RECEIVER_SOURCE_PROBE_BUFFER_SIZE}}
      grpcServerPingTime: {{RECEIVER_SOURCE_PROBE_GRPC_SERVER_PING_TIME}}
      grpcPingTimeout: {{RECEIVER_SOURCE_PROBE_GRPC_PING_TIMEOUT}}
      grpcKeepAliveMinTime: {{RECEIVER_SOURCE_PROBE_GRPC_KEEP_ALIVE_MIN_TIME}}
      grpcMaxReceiveMessageSize: {{RECEIVER_SOURCE_PROBE_GRPC_MAX_RECEIVE_MESSAGE_SIZE}}
      grpcMaxSendMessageSize: {{RECEIVER_SOURCE_PROBE_GRPC_MAX_SEND_MESSAGE_SIZE}}
