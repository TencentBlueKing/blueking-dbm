apiVersion: v1
kind: Service
metadata:
  name: {{ include "bkdata-kafka-consumer.fullname" . }}
  labels:
    {{- include "bkdata-kafka-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bkdata-kafka-consumer.selectorLabels" . | nindent 4 }}
