apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-celery-service.fullname" . }}-test-connection"
  labels:
    {{- include "db-celery-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-celery-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
