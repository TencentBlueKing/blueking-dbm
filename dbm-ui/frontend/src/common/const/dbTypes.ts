/**
 * 数据库类型
 */
export enum DBTypes {
  MYSQL = 'mysql',
  TENDBCLUSTER = 'tendbcluster', // TenDBCluster
}
export enum DBTypes {
  REDIS = 'redis',
}
export enum DBTypes {
  DORIS = 'doris',
  ES = 'es', // ElasticSearch
  HDFS = 'hdfs',
  INFLUXDB = 'influxdb',
  KAFKA = 'kafka',
  PULSAR = 'pulsar',
  RIAK = 'riak',
}
export enum DBTypes {
  MONGODB = 'mongodb',
}
export enum DBTypes {
  SQLSERVER = 'sqlserver',
}
