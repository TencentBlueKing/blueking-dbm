---
description:
  直达链接（URL 携带 `?open=` 约定参数进入页面后自动执行动作）规范。覆盖参数设计、hook 收敛、鉴权与异常兜底分支、落地清单。新增或修改直达链接入口时读。
globs: []
alwaysApply: false
---

# 直达链接（打开约定参数）规范

直达链接指通过 URL 携带约定参数，让外部渠道（单据交付通知、企微/邮件、扫码等）进入页面后自动执行某操作（打开弹窗、定位数据、聚焦表单），省掉用户手动操作。

## 参数设计

- **动作参数**：统一用 query `?open=<action>`，值为 `snake_case` 语义化命名（如 `access_entry`、`rebuild_apply`）
- **定位参数**：业务对象 id 放路由 `params`（如 `clusterId`），与 `open` 分离，复用既有跳转机制
- **单动作原则**：一个链接只表达一个动作，不叠加多个 `open` 值；确需附加信息用独立 query 键（`from` / `source` 等）

## 统一封装

判断逻辑必须收敛到一个组合式函数，禁止在各页面内联。hook 只依赖通用输入，不绑定具体业务实体或动作：

```typescript
interface DirectLinkParams<T> {
  actionId?: string; // 目标操作的 action_id；仅在动作需要鉴权时传，与按钮鉴权一致
  data: T; // 页面主体数据：从中取 id、可用状态、permission 等
  onOpen: () => void; // 鉴权通过后执行的动作回调
}
```

闭环顺序：解析并校验参数 → 判断目标对象可用性 → 鉴权（可选）→ 调 `onOpen`。

- 动作触发时机在页面数据就绪后，保证动作有完整数据支撑
- **手动入口与直达链接共享同一鉴权与动作链路**：按钮用 `AuthButton` 类组件，其
  `action-id`/`permission`/`resource` 与 hook 的 `actionId` 完全一致；`onOpen` 也走按钮那个动作函数
- 实体不存在且无独立错误码时，不做专用空态，由接口错误提示承接

## 行为分支

未传 `actionId` 视为动作无需鉴权，跳过权限判断直接执行。

| 场景                 | 行为                                  |
| -------------------- | ------------------------------------- |
| 有权限（或无需鉴权） | 自动执行目标动作（复用对应弹窗/组件） |
| 无权限               | 弹项目通用无权限申请弹窗              |
| 目标已禁用/不可用    | `messageWarn` 提示，不执行动作        |
| 未登录               | 现有登录拦截回流同一 URL 后再鉴权     |
| 参数非法/缺失        | 忽略，按普通页面进入，不得有副作用    |

## 落地清单

- [ ] `?open=` 动作值已写入接口文档/通知模板（后端据此生成下发）
- [ ] 解析 + 鉴权 + 分支收敛在 hook 内，无内联判断
- [ ] 按钮入口的 `actionId` 与 hook 一致，两条路径走同一动作函数
- [ ] 走读链路：外部链接 → 列表/详情 → 鉴权 → 自动动作，结果与手动点击一致
